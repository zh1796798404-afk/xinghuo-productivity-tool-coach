# 星火提效工具制作教练安装说明

版本：**v1.4.0**

这是一个跨平台 Agent Skill。核心行为位于 `productivity-tool-coach/SKILL.md`，对话规则和交付模板位于 `productivity-tool-coach/references/`。

## v1.4.0 的交互变化

本版本把"问卷式收集"改为"对着草稿做填空"。收到你的痛点描述和一个真实事例后，Skill 会立即在工作区生成 `提效工具方案.md` 草稿，之后每个问题都用来填草稿上的一个 `[待确认]`，每答一题当场写回。

新增或强化了：

- 开场引导：提示先把模型与推理档位调到最高，并引导清晰描述想做的工具（配示范级例子）；描述完整时草稿直出（草稿即复述）。
- 真实事例锚定：描述简短时优先问"上次是怎么做的"，从故事里直接提取输入、步骤、输出。
- 方案草稿先行：草稿全程可见，可以随时打开甚至手工修改。
- 覆盖度记分板：进度显示为"9项里7项已清晰"，不再用轮数衡量。
- 动态问题数：硬上限5问，说得越清楚问得越少；涉及多人、权限、外部对接时可进入深挖（上限10问）。
- 验收核对：交付说明以逐条"通过/未通过 + 证据"的核对表开头。
- 试用与微调闭环：交付后提供手动/自动测试选择（自动不替代上手），反馈持续微调，直到用户明确满意。
- 迭代短流程：再次调用且已有工具时，直接确认改动点，不重新开场。
- 简单高效与小白手册：工具优先图形界面、零依赖；交付手册固定"第一步/第二步"分步格式，零术语。
- 开场不再出现模式选择题；用户可见文案中"盘问"改为"梳理"。

## Codex 安装

将仓库内的 `productivity-tool-coach` 文件夹复制到以下任一位置：

```text
用户级安装：~/.agents/skills/productivity-tool-coach
```

当前 Codex 桌面环境也支持：

```text
~/.codex/skills/productivity-tool-coach
```

安装后重新打开 Codex 或新建任务，然后显式调用：

```text
$productivity-tool-coach
```

本 Skill 配置为显式调用优先，不会主动接管普通对话。

## Claude Code 安装

将 `productivity-tool-coach` 文件夹复制到：

```text
用户级安装：~/.claude/skills/productivity-tool-coach
项目级安装：<项目目录>/.claude/skills/productivity-tool-coach
```

在 Claude Code 中调用：

```text
/productivity-tool-coach
```

Claude Code 会读取 `SKILL.md` 和 `references/`。`SKILL.md` 已配置 `disable-model-invocation: true`，只响应显式调用，不会被普通对话误触发。`agents/openai.yaml` 是 Codex 的界面配置，不影响 Claude Code 的核心行为。

## 使用前提

- Skill 本身不需要额外的 Python 或 Node.js 依赖。
- 如果后续制作的是网页、脚本或其他工具，生成的工具可能需要对应运行环境。
- 建议在目标项目目录中调用：方案草稿 `提效工具方案.md` 会写入当前工作区。
- 用户可随时输入"停止梳理""直接出方案""按目前信息继续""按你的推荐来"或"先给我初版"。

## 最小使用示例

调用后可以这样开始：

```text
我每周要把十几个部门的周报复制粘贴汇总成一份总表，来回核对特别费时间。
```

描述越完整，Skill 问得越少：说全了会直接给你方案草稿；说得简短，它会先复述理解、邀请你讲一遍最近一次的实际经过，再用最多 5 个小问题把草稿填完整。工具做完后它还会陪你试用微调，直到你说满意。
